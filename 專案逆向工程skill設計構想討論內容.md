# 專案逆向工程 Skill 設計構想討論內容

> 本文件彙整「對既有無完整文件之交付專案原始碼，進行專案層逆向還原」之完整討論構想。
> 核心目的：以現有成品原始碼為基底，反向還原六大 SSDLC 各階段交付物，重建頂層系統規格書與需求追溯鏈，逆向完成後再走順向駕馭工程流程做迭代。

---

## 一、核心目的與問題定義

### 1.1 不是什麼
- **不是**逆向拆解 GitHub 開源工具 Skill
- **不是**對開源專案做程式碼審查或重構

### 1.2 是什麼
- **是**對「舊有無完整文件、歷史斷層的交付專案原始碼」做專案層逆向還原
- 以現有成品原始碼為基底，反向還原六大 SSDLC 各階段交付物：
  - 需求文件
  - 系統設計圖
  - 測試案例
  - 部署腳本
  - 維運手冊
- 自底向上回溯，重建頂層完整系統規格書、需求追溯鏈
- 逆向還原完所有階段文件後，再走原本「駕馭工程順向流程」做迭代、新增功能需求

### 1.3 解決什麼痛點
- 歷史專案文件遺失
- 流程無紀錄
- 無法正常維護改版

### 1.4 整體可行性總評
> **高度可行，屬軟體業標準舊案復原手法，搭配駕馭工程架構會大幅降低人工成本。**

---

## 二、可行優勢分析

### 2.1 架構完全相容雙層駕馭工程
內層各階段 SSDLC Skill 可分階段逆向解析程式碼產出對應交付物；逆向產生的完整階段成果可直接上拋至外層全域 Agent MD，建立標準化需求追溯鏈，後續順向迭代直接沿用既有流程。

### 2.2 不需要重新開發全新機制，重用既有 Skill 工具鏈
程式碼掃描、註解解析、自動產文件、測試覆蓋率分析、SQL 反向產 ER 圖、API 自動產規格等 GitHub 開源 Skill 全部能直接拿來做逆向解析工具，不用重寫邏輯。

### 2.3 逆向完成後直接切換順向迭代流程
舊案還原完所有階段交付文件、系統規格後，後續新增需求直接走 Plan→Generator→Evaluator 標準 PDCA 循環，舊案直接納入常態維運流程，不會產生兩套管理機制。

### 2.4 AI 僅做資訊彙整、文件自動排版，不做複雜推演
程式碼解析出來的資訊靠 GIS / 程式靜態分析工具做關聯對應，AI 只負責彙整零散資訊、自動產生標準 MD 交付文件，不會有過度依賴 AI 推演的風險。

---

## 三、落地限制與風險（必須提前處理）

### 3.1 舊原始碼品質落差大會增加人工補正工作量
若程式無註解、變數命名隨意、無單元測試、雜湊硬編碼參數，純工具逆向無法完整還原商業邏輯，需搭配開發人員口述補充。

### 3.2 第三方閉源套件、舊版廢棄依賴無法逆向內部邏輯
僅能還原自身開發程式區塊，外部商用黑箱套件只能標註依賴關係，無法拆解內部運作邏輯，系統規格需手動補充第三方元件說明。

### 3.3 歷史使用者需求、商業背景無法透過程式碼取得
程式只能反映「實作了什麼」，無法還原「當初為何這麼做」的商業需求脈絡，需搭配舊合約、使用者回饋、舊會議紀錄交叉補齊。

### 3.4 逆向產出文件會存在版本落差，必須設置人工審核閘口
工具自動生成的設計文件、需求清單會有偏差，需在各階段 Evaluator 環節增加人工確認步驟，確保逆向成果與實際系統一致。

---

## 四、完整落地執行步驟

> 分「逆向還原階段」+「順向迭代階段」兩大模組，對應六大 SSDLC。

### 模組一：逆向還原流程（舊專案原始碼 → 完整各階段交付文件 + 頂層系統規格）

全程沿用現有駕馭工程內層 PDCA 循環，僅調整 Skill 用途為「反向解析」。

#### Step 1：Plan — 規劃逆向解析範圍

**匯入素材**：
- 舊專案完整原始碼倉庫 / Git 備份
- 資料庫結構備份檔（DDL）
- 歷史部署腳本、容器組態檔
- 現有測試程式、測試資料
- 可選補充：舊合約、會議記錄、使用者回饋（人工補齊商業邏輯缺口）

**引導式勾選逆向專用 Skill（支援多選）**：

| SSDLC 階段 | 逆向子 Skill | 核心功能 |
|:-----------|:-------------|:---------|
| 需求分析 | Reverse_Skill_RequirementsRestore | 程式碼關鍵字掃描、API 路由匯整、功能模組分類、使用者場景自動歸納 |
| 系統設計 | Reverse_Skill_DesignRestore | 程式反向 ER 圖、API Spec 自動產生、模組依賴圖、系統架構圖自動繪製 |
| 開發編碼 | Reverse_Skill_CodeRestore | 程式註解匯整、函數 / 類別文件自動產生、第三方依賴清單掃描 |
| 測試驗證 | Reverse_Skill_TestRestore | 現有測試案例匯整、測試覆蓋率分析、缺失測試點標註 |
| 部署發布 | Reverse_Skill_DeployRestore | 解析 Dockerfile、Shell 部署腳本、環境參數、舊伺服器配置 |
| 維護營運 | Reverse_Skill_OpsRestore | Log 規格解析、異常處理邏輯彙整、維運操作手冊初稿生成 |

**自動衝突檢核規則（內建於 Skill）**：
- 互斥規則 1：若不匯入 DB 備份，禁止單獨執行 `Reverse_Skill_DesignRestore`
- 互斥規則 2：無原始碼倉庫時，全部逆向子 Skill 鎖定不可選
- 依賴規則：執行測試逆向 Skill 需先執行開發逆向 Skill
- 衝突提示後，使用者調整勾選清單，確認逆向執行範圍與掃描參數（掃描深度、忽略舊廢棄程式區塊）

#### Step 2：Generator — 執行逆向解析（PDCA-D）

- 依 SSDLC 順序批次執行勾選的逆向子 Skill，對原始素材做靜態掃描
- 各子 Skill 輸出原始零散結構資料（JSON、圖片、純文字 Log）
- 內建格式轉譯中介層：全部輸出統一轉換為駕馭工程標準結構，供後續檢核使用
- 階段暫存各模組逆向中間產物，避免重複掃描原始碼浪費 Token

#### Step 3：Evaluator — 檢核與分級修正（PDCA-C+A，沿用 3 次重試機制）

**Check 檢核作業（逆向專用驗證邏輯）**：

- **跨階段一致性比對**
  - 設計模組結構與程式實作是否匹配
  - API 規格與後端路由實作是否一致
  - 資料庫 ER 結構與程式 ORM 對應關係無落差
- **自動標註資訊缺口**（需人工補正項目）
  - 無註解、變數命名混亂的商業邏輯區塊
  - 第三方閉源套件、無文件外部服務依賴
  - 無法透過程式還原的歷史需求背景
- **全域彙整**所有階段解析資料，自動產出頂層系統規格草稿

**Action 分級修正機制**：

1. **第 1~3 次異常**：退回該階段 Generator，調整逆向掃描參數、補充手動備註資訊重新解析（上限 3 次）
2. **3 次重試仍無法修復缺口**：觸發人工審核閘口
   - 標記缺口清單交付開發 / 產品人員手動補充商業邏輯、歷史需求
   - 人工審核確認無誤後，輸出該階段正式 MD 交付文件

#### Step 4：全域上拋同步（逆向收尾）

所有六大階段逆向交付文件自動上傳至外層全域 Agent MD，自動完成兩件事：
1. 建立完整跨階段需求追溯鏈
2. 產生專案資產總目錄，標註「自動逆向生成」「人工補充」兩類內容區分標籤

---

### 模組二：順向迭代機制（逆向完成後切換）

- 全域 Agent MD 偵測逆向作業標記為「完成」，自動切換專案模式：由「舊案還原模式」切換為「常態迭代模式」
- 後續新增需求、改版維運，直接使用原生順向開發 Skill，不變更既有駕馭工程操作流程
- 迭代產生的新交付物自動同步更新追溯鏈，維持專案文件完整性

---

## 五、POC 驗證專用 AI 輔助範疇

**嚴格遵循規範：AI 僅彙整，不推演數值。**

- 彙整各子 Skill 掃描產出的零散資訊，自動排版標準 MD 文件
- 分類統計逆向產生的資訊缺口清單，匯總為人工補正檢查表
- 依據完整逆向交付物，自動生成對應維運、改版應變建議文字
- **禁止 AI 自動推演商業邏輯、自動補充缺失程式功能，所有缺口強制人工介入**

---

## 六、Skill 衝突與限制清單（POC 測試注意）

| # | 限制項目 | 影響 | 應對方式 |
|:-:|:---------|:-----|:---------|
| 1 | 舊原始碼品質差（無註解、雜亂硬編碼參數） | 大幅增加人工補正工作量 | 人工審核閘口 + 開發人員口述補充 |
| 2 | 第三方閉源套件 | 僅能標註依賴，無法逆向內部運作邏輯 | 系統規格手動補充第三方元件說明 |
| 3 | 歷史商業需求、專案背景 | 無法透過程式碼自動還原 | 配套舊文件交叉補齊 |
| 4 | 開源逆向工具版本更新 | 輸出格式可能錯亂 | 鎖定固定 Release 版本 |

---

## 七、POC 落地驗證檢核點（測試完成確認清單）

- [ ] 可完整掃描舊專案原始碼，分階段輸出對應交付 MD
- [ ] 衝突檢核功能正常，可攔截素材缺失、子 Skill 互斥場景
- [ ] 3 次分級重試機制正常運作，不會無限全域重跑浪費 Token
- [ ] 逆向成果可正常上拋至全域 Agent MD，自動建立需求追溯鏈
- [ ] 逆向完成後可無縫切換順向開發流程，執行新增需求迭代
- [ ] 輸出文件可明確區分自動生成 / 人工補充內容，無資訊混淆

---

## 八、機器可讀 JSON 配置對應欄位備註

此份 MD 可直接對應逆向 Skill 專用 JSON 配置，關鍵映射欄位：

| 欄位 | 值 / 說明 |
|:-----|:----------|
| `skillId` | `ReverseEngineering_LegacyRestore` |
| `skillType` | `ReverseAnalysis` |
| `phaseBind` | `["需求分析","系統分析設計","系統開發","系統測試","系統部署","系統維運"]` |
| `subSkillList` | 內嵌六大逆向子 Skill 參數 |
| `conflictRule` | 存放互斥、依賴檢核規則 |
| `retryConfig` | `maxLocalRetry=3`、`fullLoopTrigger=true` |
| `outputStandard` | `md+json` 雙格式輸出 |
| `agentSync` | `autoUpload=true`（自動同步外層全域 MD） |

---

## 九、與現有框架的銜接分析

### 9.1 逆向工程作為 Phase 00（前置階段）之定位

```
Phase 00：逆向還原（新增）
    │
    ├── Plan：匯入素材 + 勾選逆向 Skill + 衝突檢核
    ├── Generator：批次執行逆向子 Skill
    ├── Evaluator：跨階段一致性比對 + 3 次分級重試
    ├── 人工審核閘口（缺口補正）
    ├── 全域上拋同步
    │
    ▼
Phase 01：規劃與需求分析（正常使用 SSDLC 順向流程）
```

### 9.2 逆向產出物銜接 SSDLC Phase 01

| 逆向產出物 | 對應 Phase 01 輸入 |
|:-----------|:-------------------|
| `executable_spec.yaml`（逆向生成） | 直接作為 Phase 01 輸入 |
| `requirements.feature`（逆向生成） | 直接作為 Phase 01 輸入 |
| `system_specification.md`（逆向生成） | 直接作為 Phase 01 輸入 |
| `traceability_matrix.md`（逆向生成） | 直接作為 Phase 01 輸入 |

---

## 十、待討論議題

### 10.1 Phase 00 的框架定位
- 是否正式新增為 SSDLC Phase 00？
- 還是作為「可選前置作業」不納入正式階段？
- 對 `phase_gates.json` 的階段編號是否有影響？

### 10.2 逆向 Skill 與現有 Skill 的關係
- 逆向子 Skill 是獨立於現有六階段 Skill 之外？
- 還是在現有 Skill 中增加「逆向模式」參數？
- 衝突檢核機制是否共用現有 `--mode` 參數？

### 10.3 POC 測試專案選擇
- 建議先選一個小型既有專案（1000~5000 行代碼）做概念驗證
- 需具備：原始碼 + 資料庫 DDL + 部署腳本（至少三項）
- POC 完成後再評估是否擴展到中大型專案

### 10.4 人工審核閘口的 UI 介面
- 人工審核是在 Codex 對話中完成？
- 還是需要額外的檢核介面？
- 審核結果如何回饋至 Evaluator 流程？

### 10.5 與 @guide 引導機制的整合
- 逆向還原流程是否接入 `@guide reverse` 引導？
- 五關卡引導結構如何調整以適配逆向工程場景？

---

## 十一、實作優先級建議

### 高優先級（核心功能，POC 必做）
1. `Reverse_Skill_RequirementsRestore`（需求逆向）
2. `Reverse_Skill_DesignRestore`（設計逆向，含 DB + API）
3. Evaluator 跨階段一致性比對機制
4. 與 Phase 01 的輸出物銜接驗證

### 中優先級（擴充功能，POC 後評估）
5. `Reverse_Skill_CodeRestore`（開發逆向）
6. `Reverse_Skill_TestRestore`（測試逆向）
7. 互斥 / 依賴衝突檢核規則

### 低優先級（優化功能，正式版再做）
8. `Reverse_Skill_DeployRestore`（部署逆向）
9. `Reverse_Skill_OpsRestore`（維運逆向）
10. 模式自動切換（逆向完成 → 順向迭代）

---

## 十二、下一步行動

| # | 行動項目 | 負責方 | 預計時程 |
|:-:|:---------|:-------|:---------|
| 1 | 確認逆向工程流程是否新增為 Phase 00 | 使用者 | 待定 |
| 2 | 選定小型測試專案做 POC 概念驗證 | 使用者 | 待定 |
| 3 | 設計 `Reverse_Skill_RequirementsRestore` SKILL.md 規格 | AI 代理 | 待 Phase 00 定案後 |
| 4 | 設計逆向專用 JSON 配置結構 | AI 代理 | 待 Phase 00 定案後 |
| 5 | 評估開源逆向工具（SQL 反向 ER、API 自動產規格等）的可用性 | 使用者 + AI | 並行進行 |

---

> 本文件為討論草稿，歡迎補充修正。
> 整合來源：原始構想討論 + 落地執行步驟 + POC 驗證規劃
> 最後更新：2026-07-16
