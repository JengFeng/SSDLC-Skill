---
name: 00_cross_phase
description: 跨階段全域共用技能。適用於所有 SSDLC 開發階段的通用工具，包含版本控制、多 Agent 協作、程式碼差異同步、自主迭代研究與 TDD 流程。
---

# 跨階段全域共用技能 (00_cross_phase)

> 📌 本階段的完整 PDCA 規範定義於框架 `.agents/skills/00_cross_phase/SKILL.md`。本檔案為專案層級整合定義，Skill 導入後自動合併。

## 已導入 Skill

> 尚無 Skill 導入。請使用 `@00` 查詢可用 Skill 並以 `@00/XX` 導入。

## 安全防護整合（條件式）

> 本節僅在 `phase_gates.json` 中 `security_baseline.enabled` 為 `true` 時啟用。
