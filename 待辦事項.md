# 待辦事項 (Framework TODO)

> 建立日期：2026-06-29 | 來源：Phase 02 執行實戰回饋
> 用途：記錄 AI 協作過程中發現的框架定義模糊或缺失，供後續架構優化參考。

---

## 🔴 TODO-001：Skill 快捷編號格式需在 `commands_reference.md` 加入明確範例防呆

**發現情境**：`@02` 列出 Skill 清單時，AI 代理擅自加上 `S` 前綴（如 `S01`, `S02`），但規章明確定義快捷編號為純雙位數數字 `01`, `02`, `03`...。

**現況分析**：
- `.agents/AGENTS.md` 第 199 行已明確定義：「分配雙位數快捷編號（由 `01` 開始，如 `01`, `02`, `03` ...）」
- `docs/commands_reference.md` 指令表中範例為 `@02/01,02`
- demo_project 實際使用記錄亦為純數字：`@02/03,10,12`
- **但**：目前沒有「禁止加前綴字母」的**明確防呆條款**，導致 AI 代理可能自行發揮（如 `S01`）

**建議修正方向**：
1. 在 `.agents/AGENTS.md` 快捷編號分配規則中，新增一行：
   > 「嚴禁在快捷編號前加上任何字母前綴（如 `S`、`No.`、`#` 等），僅允許純雙位數數字。」
2. 在 `docs/commands_reference.md` 的參數防呆段落中，增加此違規案例的錯誤提示。

---

### 📐 建議調整方式（具體實作）

#### 調整 1：`.agents/AGENTS.md`

**檔案**：`e:\00AI協作\SSDLC-skill\.agents\AGENTS.md`
**位置**：第二章第 2 節「階段 Skill 查詢指令」→ 第 2 點「快捷編號分配」

**現行內容**（約第 199 行）：
```
2.  **快捷編號分配**：AI 代理必須按字母/數字順序對掃描出的所有可用 Skill 進行排序，並為其分配雙位數快捷編號（由 `01` 開始，如 `01`, `02`, `03` ...）。
```

**修改為**：
```
2.  **快捷編號分配**：AI 代理必須按字母/數字順序對掃描出的所有可用 Skill 進行排序，並為其分配雙位數快捷編號（由 `01` 開始，如 `01`, `02`, `03` ...）。
    *   **⚠️ 防呆**：嚴禁在快捷編號前加上任何字母前綴（如 `S01`、`No.01`、`#01` 等），僅允許純雙位數數字。若 AI 代理輸出時誤加前綴，視為格式錯誤，必須立即修正後重新輸出。
```

---

#### 調整 2：`docs/commands_reference.md`

**檔案**：`e:\00AI協作\SSDLC-skill\docs\commands_reference.md`
**位置**：第二章「參數防呆與錯誤處理規則」→ 第 1 節「快捷編號與逗號 (,) 防呆提醒」

**現行內容**（約第 50 行附近，`錯誤提醒與友好提示` 段落結尾）：
```
    *   **無效快捷編號錯誤提醒**：若其中有任何一個快捷編號在該開發階段中不存在...
```

**在此段落之前新增一條**：
```
    *   **前綴字母錯誤提醒**：若 AI 代理在輸出快捷編號清單時誤加字母前綴（如 `S01`、`S02`），系統必須自我修正，移除所有前綴字母，僅保留純雙位數數字（`01`、`02`）。此為 AI 代理內部自我檢核，不需對使用者顯示錯誤訊息。
```

---

#### 調整 3：`skills/README.md`（可選）

**檔案**：`e:\00AI協作\SSDLC-skill\skills\README.md`
**建議**：在目錄索引表中的每個階段 Skill 清單旁，加入快捷編號範例欄位，讓 AI 代理有明確參照：

```
| 階段 | Skill | 快捷編號（範例） |
|:---|:---|:---|
| 02 | mermaid | 10 |
| 02 | plantuml | 12 |
```

**影響範圍**：所有 `@[階段]` 查詢與 `@[階段]/[編號]` 導入指令。

**優先級**：🔴 高（導致使用者無法正確選取 Skill）

---

## 🔴 TODO-002：Mermaid + PlantUML 雙 Skill 選取時需強制產出雙格式圖表

**發現情境**：使用者 `@02/...` 同時選取了 mermaid(10) 與 plantuml(12)，但 AI 代理僅以 PlantUML 產出 UML 圖表（用例圖/活動圖/時序圖），未產出 Mermaid 版本。ER 圖則僅以 Mermaid 產出。使用者期望兩種格式都應產出。

**現況分析**：
- SKILLS歸類.md 定義：
  - mermaid → 「架構圖、流程圖」
  - plantuml → 「UML 圖表」
- 兩者職責邊界模糊：mermaid 也可畫 ER 圖、時序圖、流程圖；plantuml 也可畫 ER 圖
- **缺乏「多 Skill 聯合產出時的覆蓋規則」**：當多個同質 Skill 同時選取時，應全部產出而非擇一

**建議修正方向**：
1. 在 `.agents/AGENTS.md` 或 Phase 02 `SKILL.md` 中，新增「**多 Skill 聯合產出規則**」
2. 各圖表輸出檔應以 Mermaid 格式為優先（Markdown 原生支援），PlantUML 為補充
3. 在 Phase 02 產出清單中明確定義每種圖表的格式要求（如下表）

| 圖表 | 預設格式 | 選取 plantuml 時追加 | 選取 mermaid 時追加 |
|:---|:---|:---|:---|
| ER 圖 | Mermaid (erDiagram) | PlantUML 版本 | —（已是 Mermaid） |
| 用例圖 | Mermaid (flowchart) | PlantUML 版本 | —（已是 Mermaid） |
| 活動圖 | Mermaid (flowchart) | PlantUML 版本 | —（已是 Mermaid） |
| 時序圖 | Mermaid (sequenceDiagram) | PlantUML 版本 | —（已是 Mermaid） |
| 架構圖 | Mermaid (flowchart) | PlantUML 版本 | —（已是 Mermaid） |

---

### 📐 建議調整方式（具體實作）

#### 調整 1：`.agents/AGENTS.md` 新增「多 Skill 聯合產出規則」

**檔案**：`e:\00AI協作\SSDLC-skill\.agents\AGENTS.md`
**位置**：第三章第 3 節「直接與聯合導入 Skill 指令」→ 第 4 點「檔案部署」之後，新增第 4.5 點

**新增內容**：
```
    4.5  **多 Skill 聯合產出規則（Multi-Skill Output Rule）**：
        *   當同一階段選取多個具備重疊輸出能力的 Skill（例如 mermaid 與 plantuml 皆可繪製圖表），Generator 必須為**每一種目標圖表類型**產出**所有選取 Skill 支援的格式**。
        *   **禁止擇一產出**：不得因已用 PlantUML 繪製時序圖而省略 Mermaid 版本，反之亦然。
        *   **格式優先序**：同一輸出檔內，Mermaid 格式置於 PlantUML 之前（因 Markdown 原生支援 Mermaid 渲染）。
        *   **對照表**：以下為 Phase 02 圖表產出格式強制對照表，Generator 必須嚴格遵守：

        | 圖表類型 | 輸出檔 | 必備格式 | 選取 plantuml 時追加 | 選取 mermaid 時追加 |
        |:---|:---|:---|:---|:---|
        | ER 圖 | er_diagram.md | Mermaid (erDiagram) | PlantUML (@startuml) | —（已為 Mermaid） |
        | 用例圖 | use_case_diagram.md | Mermaid (flowchart) | PlantUML (@startuml) | —（已為 Mermaid） |
        | 活動圖 | activity_diagram.md | Mermaid (flowchart) | PlantUML (@startuml) | —（已為 Mermaid） |
        | 時序圖 | sequence_diagram.md | Mermaid (sequenceDiagram) | PlantUML (@startuml) | —（已為 Mermaid） |
        | 架構圖 | design_brief.md 內 | Mermaid (flowchart) | PlantUML (@startuml) | —（已為 Mermaid） |

        *   **Evaluator 檢查點**：若選取了 mermaid + plantuml，但任一圖表輸出檔僅含一種格式，Evaluator 判定為 B 類錯誤（規劃層根源錯誤），強制退回 Generator 補齊。
```

---

#### 調整 2：Phase 02 `SKILL.md` 模板更新

**檔案**：`e:\00AI協作\SSDLC-skill\skills\02_system_design\SKILL.md`（框架層模板）
**位置**：在產出清單段落中

**新增內容**（在產出清單表格下方）：
```
> ⚠️ **雙格式強制規則**：若同時選取 mermaid 與 plantuml，所有圖表輸出檔（er_diagram.md / use_case_diagram.md / activity_diagram.md / sequence_diagram.md）必須**同時包含** Mermaid 與 PlantUML 兩種格式的原始碼。Mermaid 在前、PlantUML 在後。僅產出單一格式者，Evaluator 判定為 B 類錯誤。
```

---

#### 調整 3：`docs/commands_reference.md` 參數防呆補充

**檔案**：`e:\00AI協作\SSDLC-skill\docs\commands_reference.md`
**位置**：第一章核心指令表中 `@[階段]/[快捷編號_1],[快捷編號_2]` 的說明欄

**現行內容**：
```
| **`@[階段]/[快捷編號_1],[快捷編號_2]`** | 逗號相連的多個快捷編號 | 聯合導入。依次部署多個 Skill...
```

**修改為**（在說明中加入）：
```
...聯合導入。依次部署多個 Skill，並將它們的 instructions 以各自標題獨立封裝追加合併至目標 `SKILL.md`。**⚠️ 多 Skill 聯合產出規則**：若選取的 Skill 之間存在重疊輸出能力（如 mermaid + plantuml 皆可繪圖），Generator 必須為每種圖表產出所有格式，禁止擇一。
```

**影響範圍**：Phase 02 所有圖表產出。

**優先級**：🔴 高（產出格式不完整，需人工補救）

---

## 🟡 TODO-003：`specs/executable_spec.yaml` 與 `specs/features/requirements.feature` 未在 Phase 01 自動填入

**發現情境**：Phase 02 啟動時，spec_ref.md 要求讀取這兩個檔案，但它們仍為空白模板。

**建議**：Phase 01 Evaluator 通過後，應自動將正規化需求同步寫入 `executable_spec.yaml` 的 `requirements` 段落，並將接受準則轉為 `requirements.feature` 中的 Gherkin 場景。

---

### 📐 建議調整方式（具體實作）

#### 調整 1：`.agents/AGENTS.md` CORE_RULES 三-2 節補強

**檔案**：`e:\00AI協作\SSDLC-skill\.agents\AGENTS.md`
**位置**：第二章第 2.2 節「SSOT 三軌規格架構」之後

**新增內容**：
```
    2.2.5  **Phase 01 完成後自動同步 SSOT 規則**：
        *   Phase 01 Evaluator 通過後，AI 代理必須自動執行以下同步：
            1.  將 formal_requirements.md 中的 FR/NFR 清單寫入 executable_spec.yaml 的 phase_01_planning.requirements 段落。
            2.  將 formal_requirements.md 中的接受準則 (Acceptance Criteria) 轉換為 Gherkin 語法（Given/When/Then），寫入 specs/features/requirements.feature。
            3.  更新 executable_spec.yaml 的 project.last_updated 與 version。
        *   此為強制步驟，未執行者 Evaluator 於 Phase 02 Checkpoint A 偵測到空白規格時，判定為 B 類錯誤。
```

#### 調整 2：`scripts/` 新增自動同步腳本（可選）

**檔案**：`e:\00AI協作\SSDLC-skill\scripts\sync_ssot_phase01.py`
**功能**：讀取 `formal_requirements.md` → 解析 FR/NFR → 寫入 `executable_spec.yaml` → 生成 `requirements.feature`

**優先級**：🟡 中

---

## 🟢 TODO-004：Phase 02 完成後應自動建立 Baseline

**發現情境**：Phase 01 和 Phase 02 完成後，`phase_gates.json` 中 `baseline` 仍為 `null`，未自動觸發 `@baseline`。

**建議**：Evaluator 通過後自動觸發 `@baseline`（依 CORE_RULES.md 三-4 規範）。

---

### 📐 建議調整方式（具體實作）

#### 調整 1：`.agents/AGENTS.md` Evaluator 段落補強

**檔案**：`e:\00AI協作\SSDLC-skill\.agents\AGENTS.md`
**位置**：第二章第 4-5 節「Evaluator 通過後觸發全域同步」

**現行內容**（約第 4-5 節）已有觸發清單，但缺少 Baseline 建立步驟。在現有 3 項觸發之後，新增第 4 項：

**新增內容**：
```
    4.  **自動建立 Baseline**：Evaluator 通過且 SSOT 檢查 (check_spec_integrity.py --mode B) 全部通過後，AI 代理自動執行 @baseline 指令，建立本階段穩定快照至 baseline/phase-{N}_v{M}/。
        *   若 baseline/ 目錄不存在，自動建立。
        *   版本號自動遞增（v1 → v2 → ...）。
        *   保留最近 3 份 Baseline，舊版自動清理。
        *   建立完成後自動執行基線可執行性驗證（參考 commands_reference.md 第二章第 5 節）。
```

#### 調整 2：`docs/commands_reference.md` 補充

**檔案**：`e:\00AI協作\SSDLC-skill\docs\commands_reference.md`
**位置**：第一章 `@baseline` 指令的「系統行為」欄

**現行內容**：
```
| **`@baseline`** | 無 | 建立可獨立執行的完整專案快照...
```

**修改為**（在說明末端加入自動觸發條件）：
```
...保留最近 3 份，舊版自動清理。**🔄 自動觸發**：每次階段 Evaluator 通過後自動執行，不需使用者手動呼叫。
```

**優先級**：🟢 低（已有手動 `@baseline` 指令）

---

## 🔴 TODO-005：UI 雛形產出時未強制交叉比對全量需求，導致頁面缺漏

**發現情境**：Phase 02 Generator 產出的 `ui_prototype.html` v1 僅包含 7 個畫面（儀表板/列表/新增/編輯/報表/匯出/部門/稽核），但遺漏了以下需求對應的畫面：
- ❌ 登入頁面（AD SSO + 本機備援）→ NFR-004, REQ-005
- ❌ 員工自助服務 ESS 頁（`tab-profile` 被 topbar 引用但未實作）→ REQ-004
- ❌ 員工明細唯讀檢視頁 → REQ-001
- ❌ 異動記錄/學歷/經歷/證照的「新增表單」（僅有按鈕無 Modal）→ REQ-002, REQ-003
- ❌ 附件上傳介面（拖曳上傳 + 檔案清單）→ REQ-003
- ❌ 報表篩選控制項（年度/月份/部門下拉）→ REQ-006

使用者人工檢視後才發現缺漏，需 AI 代理重新補齊（v1 → v2）。

**現況分析**：
- 現有框架中，Generator 產出 UI 雛形時**沒有強制步驟**要求逐一比對 `formal_requirements.md` 中的每一項 FR/NFR，確認所有需求都有對應的 UI 畫面。
- Generator 的核心鐵律是「只執行、不判斷、不檢查」，但**「不檢查」不應等同於「不確認需求覆蓋」**——產出前的需求覆蓋確認應屬於 Generator 的執行範圍，而非 Evaluator 事後偵錯。
- 若 Evaluator 也未檢查 UI 畫面與需求的對應關係，則缺漏會直接流入下階段。

**建議修正方向**：
1. 在 Phase 02 Generator 產出 UI 雛形前，強制執行「需求覆蓋檢查清單」
2. 在 Evaluator 檢查點中加入 UI 畫面 × FR 對應矩陣驗證

---

### 📐 建議調整方式（具體實作）

#### 調整 1：`.agents/AGENTS.md` Phase 02 Generator 段落加入 UI 需求覆蓋強制清單

**檔案**：`e:\00AI協作\SSDLC-skill\.agents\AGENTS.md`
**位置**：Phase 02 Generator 執行規範段落（或 Phase 02 `SKILL.md`）

**新增內容**：
```
    **UI 雛形需求覆蓋強制檢查清單**：
    Generator 在產出 ui_prototype.html 之前，必須逐項比對 formal_requirements.md 中的所有功能需求，確認以下畫面全部存在：

    | FR | 必須包含的畫面/元件 | 檢查 |
    |:---|:---|:---:|
    | REQ-001 員工主檔 CRUD | 列表頁 + 新增表單 + 編輯表單 + 唯讀檢視頁 + 刪除確認 | ☐ |
    | REQ-002 生命週期管理 | 異動歷程列表 + 新增異動 Modal/表單 | ☐ |
    | REQ-003 學經歷與證照 | 學歷列表+Modal + 經歷列表+Modal + 證照列表+Modal + 附件上傳區 | ☐ |
    | REQ-004 員工自助 ESS | 個人資料頁（唯讀欄位 + 可編輯聯絡欄位標示） | ☐ |
    | REQ-005 RBAC 權控 | 三種角色的 Sidebar/欄位可見性差異（雛形需可切換展示） | ☐ |
    | REQ-006 人事報表 | 報表篩選控制項（年度/月份/部門） + 年資圖 + 部門圖 + 壽星表 + 離職率圖 | ☐ |
    | REQ-007 資料匯出 | 匯出頁面（含篩選 + 四種匯出類型） | ☐ |
    | NFR-004 AD SSO | 登入頁面（AD SSO 按鈕 + 本機備援表單） | ☐ |
    | NFR-003 稽核日誌 | 稽核日誌查詢頁面（含篩選 + 異動前後內容欄位） | ☐ |

    **⚠️ 以上清單全部勾選後方可提交 Evaluator。任一未勾選者，視為 Generator 未完成執行。**
```

#### 調整 2：Evaluator Phase 02 檢查點加入 UI 覆蓋稽核

**檔案**：`e:\00AI協作\SSDLC-skill\.agents\AGENTS.md`
**位置**：Evaluator Phase 02 審查項目

**新增內容**：
```
    **UI 需求覆蓋稽核**：Evaluator 必須打開 ui_prototype.html，搜尋以下關鍵 id/class，確認對應畫面存在：
    - `tab-login` 或 `login-page`（登入頁）
    - `tab-profile`（ESS 自助頁）
    - `tab-view-employee`（唯讀檢視頁）
    - `modalAddHistory` / `modalAddEducation` / `modalAddExperience` / `modalAddCert`（新增表單 Modal）
    - 檔案上傳 `<input type="file">` 元件
    - 報表篩選 `<select>` 下拉元件
    若任一缺失，判定為 B 類錯誤，退回 Generator 補齊。
```

**影響範圍**：Phase 02 UI 雛形產出。

**優先級**：🔴 高（產出缺漏需人工發現並補救，違反自動化初衷）

---

## 🔴 TODO-006：`check_spec_integrity.py` 路徑寫死 `demo_project/`，無法驗證其他專案

**發現情境**：在 `myPrj/` 專案完成 Phase 02 後執行 `check_spec_integrity.py --mode B`，報表顯示 24/24 通過。但進一步檢視程式碼發現，所有檢查路徑都**寫死指向 `demo_project/`**：

```python
# 第 44-48 行：檢查點 A 的檔案路徑全部寫死
files = [
    "demo_project/specs/executable_spec.yaml",
    "demo_project/specs/features/requirements.feature",
    ...
]

# 第 110 行：檢查點 B 的產出目錄也寫死
base = os.path.join(ROOT, "demo_project", dir_name)

# 第 130 行：Mermaid 檢查也寫死
diagram_dir = os.path.join(ROOT, "demo_project", "02_system_design", "outputs")

# 第 148 行：追溯鏈檢查也寫死
rtm_path = os.path.join(ROOT, "demo_project", "01_planning_and_analysis", "reg", ...)
```

**現況分析**：
- 整個腳本中「demo_project」出現 **14 次**，全為硬編碼字串
- 執行 `--mode B` 時實際檢查的是 `demo_project/` 的產出，而非目前正在開發的 `myPrj/`
- 這導致 SSOT 驗證報表**完全不反映當前專案的真實狀態**
- Phase 03 後的檢查（`app.py`、`templates/` 等）也因為檢查 `demo_project/` 而虛假通過

**影響**：所有非 `demo_project` 的專案（如 `myPrj`）執行 SSOT 檢查時得到的都是「假陽性」結果，無法真正發揮防線作用。

---

### 📐 建議調整方式（具體實作）

#### 調整 1：改為自動偵測或接受參數

**檔案**：`e:\00AI協作\SSDLC-skill\scripts\check_spec_integrity.py`
**修改範圍**：全檔案

**方案 A（接受 CLI 參數）**：
```python
parser.add_argument("--project", default="demo_project", help="目標專案目錄名稱")
```
將所有 `"demo_project"` 字串替換為 `args.project`。

**方案 B（自動偵測當前目錄）**：
```python
# 從當前工作目錄向上尋找最近的 SSDLC 專案根目錄
cwd = os.getcwd()
while cwd != os.path.dirname(cwd):
    if os.path.exists(os.path.join(cwd, "traceability_matrix.md")) and \
       os.path.exists(os.path.join(cwd, "system_specification.md")):
        ROOT = cwd
        break
    cwd = os.path.dirname(cwd)
```

**方案 C（兩者結合）**：接受 `--project` 參數，未指定時自動偵測當前目錄。這是推薦做法。

#### 調整 2：`.agents/AGENTS.md` 中的檢查指令更新

**檔案**：`e:\00AI協作\SSDLC-skill\.agents\AGENTS.md`
**位置**：提到 `check_spec_integrity.py` 的所有段落

將：
```
python scripts/check_spec_integrity.py --mode B
```
更新為：
```
python scripts/check_spec_integrity.py --project myPrj --mode B
```
或（若採用方案 B 自動偵測）：
```
cd myPrj && python ../scripts/check_spec_integrity.py --mode B
```

**優先級**：🔴 高（SSOT 防線對非 demo 專案完全失效，屬於系統性缺陷）

---

> 📌 以上待辦事項將於後續 `@optimize` 或框架維護時逐一檢討與修正。
